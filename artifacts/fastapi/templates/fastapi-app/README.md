# FastAPI app template

Minimal FastAPI project scaffold.

## Usage

Create a new project from this template using the ProjectDev MCP server's `create_project` tool with `template_id: "fastapi-app"`.
